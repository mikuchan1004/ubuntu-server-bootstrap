sudo tee /etc/update-motd.d/99-custom >/dev/null <<'EOF'
#!/usr/bin/env bash
# Custom MOTD (Korean) - server status summary
set -euo pipefail

# 로그인 IP (SSH 접속 기준)
if [[ -n "${SSH_CLIENT:-}" ]]; then
  CLIENT_IP="${SSH_CLIENT%% *}"
else
  CLIENT_IP="(콘솔/로컬)"
fi

# 로그인 사용자 / 현재 권한
LOGIN_USER="${SUDO_USER:-${USER:-unknown}}"
RUN_AS="$(whoami)"

# 디스크 / 메모리
DISK_FREE="$(df -h / | awk 'NR==2 {print $4}')"
MEM_AVAIL="$(awk '/MemAvailable:/ {
  kb=$2
  mib=kb/1024
  gib=mib/1024
  if (gib>=1) printf "%.1fGiB",gib
  else printf "%.0fMiB",mib
  exit
}' /proc/meminfo)"

echo "========================================"
echo " 🖥  서버 상태 요약"
echo "----------------------------------------"
echo " 📅 현재 시간  : $(date '+%Y-%m-%d %H:%M:%S')"

if [[ "$LOGIN_USER" == "$RUN_AS" ]]; then
  echo " 👤 로그인 계정: ${LOGIN_USER}"
else
  echo " 👤 로그인 계정: ${LOGIN_USER} (현재 권한: ${RUN_AS})"
fi

echo " 🌐 접속 IP    : ${CLIENT_IP}"
echo " ⏱  업타임     : $(uptime -p)"
echo " 💾 디스크(/)  : ${DISK_FREE} 여유"
echo " 🧠 메모리     : ${MEM_AVAIL} 여유"

# Fail2Ban (root만)
if [[ "$(id -u)" -eq 0 ]]; then
  BANNED="$(timeout 1 fail2ban-client status sshd 2>/dev/null | awk -F': ' '/Currently banned/ {print $2}')"
  [[ -n "$BANNED" ]] || BANNED="0"
  echo " 🔐 Fail2Ban   : ${BANNED} IP 차단 중"
else
  echo " 🔐 Fail2Ban   : (권한 없음)"
fi

echo "========================================"
EOF

sudo chmod 0755 /etc/update-motd.d/99-custom
